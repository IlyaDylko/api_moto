import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  navigationContainer: {
    flexDirection: 'row',
  },
  container: {
    justifyContent: 'center',
    alignItems: 'center'
  }
});